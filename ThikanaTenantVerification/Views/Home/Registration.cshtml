<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>নিবন্ধন - ঠিকানা যাচাইকরণ</title>
    <link href="https://fonts.maateen.me/solaiman-lipi/font.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --police-blue: #0d2b6c;
            --police-red: #e4002b;
            --police-gold: #ffc107;
            --police-light-blue: #1e88e5;
            --dark-bg: #0a1a36;
            --light-bg: #f0f4f8;
            --white: #ffffff;
            --text-dark: #1e293b;
            --text-light: #64748b;
            --success-color: #10b981;
            --error-color: #ef4444;
            --gradient-blue: linear-gradient(135deg, #0d2b6c 0%, #1e3a8a 100%);
            --gradient-gold: linear-gradient(135deg, #ffc107 0%, #ffd54f 100%);
            --shadow-strong: 0 20px 60px rgba(0, 0, 0, 0.3);
            --shadow-light: 0 8px 30px rgba(0, 0, 0, 0.1);
            --radius-lg: 20px;
            --radius-md: 12px;
            --radius-sm: 8px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'SolaimanLipi', 'Inter', sans-serif;
            background: var(--gradient-blue);
            color: var(--text-dark);
            line-height: 1.6;
            min-height: 100vh;
        }

        /* Compact Header */
        .police-header {
            background: rgba(13, 43, 108, 0.98);
            backdrop-filter: blur(10px);
            border-bottom: 2px solid var(--police-red);
            position: sticky;
            top: 0;
            z-index: 1000;
            padding: 0.6rem 0;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        .header-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            text-decoration: none;
        }

        .police-emblem {
            width: 45px;
            height: 45px;
            background: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid var(--police-gold);
        }

            .police-emblem i {
                color: var(--police-blue);
                font-size: 1.4rem;
            }

        .logo-text {
            color: var(--white);
            line-height: 1.2;
        }

        .logo-main {
            font-size: 1.3rem;
            font-weight: 700;
            display: block;
        }

        .logo-sub {
            font-size: 0.8rem;
            opacity: 0.9;
        }

        /* Main Content */
        .main-content {
            min-height: calc(100vh - 120px);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
        }

        /* Registration Container */
        .registration-container {
            width: 100%;
            max-width: 500px;
        }

        .registration-card {
            background: var(--white);
            border-radius: var(--radius-lg);
            padding: 2.5rem;
            box-shadow: var(--shadow-strong);
        }

        /* Header */
        .registration-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .registration-icon {
            width: 80px;
            height: 80px;
            background: var(--gradient-gold);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            font-size: 2.5rem;
            color: var(--police-blue);
        }

        .registration-title {
            font-size: 1.8rem;
            color: var(--police-blue);
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .registration-subtitle {
            color: var(--text-light);
        }

        /* Form */
        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-weight: 600;
            color: var(--police-blue);
            margin-bottom: 0.5rem;
            font-size: 0.95rem;
        }

            .form-label i {
                color: var(--police-red);
            }

        .input-group {
            position: relative;
        }

            .input-group i {
                position: absolute;
                left: 1rem;
                top: 50%;
                transform: translateY(-50%);
                color: var(--text-light);
                font-size: 1.1rem;
            }

        .form-control {
            width: 100%;
            padding: 0.875rem 1rem 0.875rem 3rem;
            border: 2px solid #e2e8f0;
            border-radius: var(--radius-sm);
            font-size: 1rem;
            transition: all 0.3s ease;
        }

            .form-control:focus {
                outline: none;
                border-color: var(--police-blue);
                box-shadow: 0 0 0 3px rgba(13, 43, 108, 0.1);
            }

        /* Demo Credentials */
        .demo-credentials {
            background: rgba(13, 43, 108, 0.05);
            border-radius: var(--radius-md);
            padding: 1.5rem;
            margin: 1.5rem 0;
            border: 1px solid rgba(13, 43, 108, 0.1);
        }

        .demo-header {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 1rem;
            color: var(--police-blue);
            font-weight: 600;
        }

        .demo-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .demo-item {
            background: var(--white);
            padding: 1rem;
            border-radius: var(--radius-sm);
            border: 1px solid #e2e8f0;
            cursor: pointer;
            transition: all 0.3s ease;
        }

            .demo-item:hover {
                transform: translateY(-2px);
                box-shadow: var(--shadow-light);
                border-color: var(--police-blue);
            }

        .demo-label {
            font-size: 0.8rem;
            color: var(--text-light);
            margin-bottom: 0.5rem;
        }

        .demo-value {
            font-weight: 700;
            color: var(--police-blue);
            font-family: 'Courier New', monospace;
        }

        /* Buttons */
        .action-buttons {
            display: flex;
            gap: 1rem;
            margin-top: 2rem;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            padding: 0.875rem 1.75rem;
            border-radius: var(--radius-md);
            font-weight: 600;
            font-size: 1rem;
            text-decoration: none;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            flex: 1;
        }

        .btn-outline {
            background: transparent;
            color: var(--police-blue);
            border: 2px solid var(--police-blue);
        }

            .btn-outline:hover {
                background: var(--police-blue);
                color: var(--white);
            }

        .btn-primary {
            background: var(--gradient-blue);
            color: var(--white);
            box-shadow: 0 10px 25px rgba(13, 43, 108, 0.3);
        }

            .btn-primary:hover {
                transform: translateY(-2px);
                box-shadow: 0 15px 35px rgba(13, 43, 108, 0.4);
            }

        /* Alert */
        .alert {
            padding: 1rem;
            border-radius: var(--radius-sm);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            animation: slideIn 0.5s ease;
        }

        .alert-danger {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            color: var(--error-color);
        }

        .alert-success {
            background: rgba(16, 185, 129, 0.1);
            border: 1px solid rgba(16, 185, 129, 0.2);
            color: var(--success-color);
        }

        /* Login Link */
        .login-link {
            text-align: center;
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 1px solid #e2e8f0;
            color: var(--text-light);
            font-size: 0.9rem;
        }

            .login-link a {
                color: var(--police-blue);
                font-weight: 600;
                text-decoration: none;
            }

        /* Footer */
        .registration-footer {
            background: var(--dark-bg);
            color: var(--white);
            padding: 1rem 0;
            text-align: center;
            font-size: 0.85rem;
        }

        /* Animations */
        @@keyframes slideIn {
            from

        {
            opacity: 0;
            transform: translateX(-20px);
        }

        to {
            opacity: 1;
            transform: translateX(0);
        }

        }

        /* Responsive */
        @@media (max-width: 768px) {
            .registration-card

        {
            padding: 1.5rem;
        }

        .demo-grid {
            grid-template-columns: 1fr;
        }

        .action-buttons {
            flex-direction: column;
        }

        .btn {
            width: 100%;
        }

        .header-container {
            padding: 0 1rem;
        }

        }

        @@media (max-height: 768px) {
            .main-content

        {
            padding: 1rem;
            min-height: calc(100vh - 100px);
        }

        .registration-card {
            padding: 1.5rem;
        }

        }
    </style>
</head>
<body>
    <!-- Compact Header -->
    <header class="police-header">
        <div class="header-container">
            <a href="/" class="logo-container">
                <div class="police-emblem">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <div class="logo-text">
                    <span class="logo-main">ঠিকানা যাচাইকরণ</span>
                    <span class="logo-sub">বাংলাদেশ পুলিশ</span>
                </div>
            </a>

            <a href="@Url.Action("Login", "Home")" class="btn btn-outline" style="padding: 0.5rem 1rem; font-size: 0.9rem;">
                <i class="fas fa-sign-in-alt"></i>
                লগইন
            </a>
        </div>
    </header>

    <!-- Main Content -->
    <main class="main-content">
        <div class="registration-container">
            <div class="registration-card">
                <div class="registration-header">
                    <div class="registration-icon">
                        <i class="fas fa-user-plus"></i>
                    </div>
                    <h2 class="registration-title">নতুন নিবন্ধন</h2>
                    <p class="registration-subtitle">আপনার জাতীয় পরিচয়পত্র ও মোবাইল নম্বর দিন</p>
                </div>

                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger">
                        <i class="fas fa-exclamation-circle"></i>
                        @ViewBag.Error
                    </div>
                }

                <form method="post" action="@Url.Action("Login", "Home")">
                    <div class="form-group">
                        <label class="form-label">
                            <i class="fas fa-id-card"></i>
                            জাতীয় পরিচয়পত্র নম্বর
                        </label>
                        <div class="input-group">
                            <i class="fas fa-id-card"></i>
                            <input type="text"
                                   name="idNumber"
                                   class="form-control"
                                   placeholder="১০, ১৩, ১৪ বা ১৭ ডিজিটের নম্বর"
                                   pattern="[0-9]{10}|[0-9]{13}|[0-9]{14}|[0-9]{17}"
                                   required>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="form-label">
                            <i class="fas fa-mobile-alt"></i>
                            মোবাইল নম্বর
                        </label>
                        <div class="input-group">
                            <i class="fas fa-phone"></i>
                            <input type="tel"
                                   name="mobileNumber"
                                   class="form-control"
                                   placeholder="০১XXXXXXXXX"
                                   pattern="01[3-9]\d{8}"
                                   required>
                        </div>
                    </div>

                    <!-- Demo Credentials -->
                    <div class="demo-credentials">
                        <div class="demo-header">
                            <i class="fas fa-user-secret"></i>
                            <span>ডেমো একাউন্ট</span>
                        </div>
                        <div class="demo-grid">
                            <div class="demo-item" onclick="fillDemoCredentials('1990123456789', '01712345678')">
                                <div class="demo-label">ডেমো একাউন্ট ১</div>
                                <div class="demo-value">NID: 1990123456789</div>
                            </div>
                            <div class="demo-item" onclick="fillDemoCredentials('1991123456789', '01812345678')">
                                <div class="demo-label">ডেমো একাউন্ট ২</div>
                                <div class="demo-value">NID: 1991123456789</div>
                            </div>
                        </div>
                        <p style="margin-top: 1rem; font-size: 0.85rem; color: var(--text-light);">
                            যেকোনো ১১ ডিজিটের মোবাইল নম্বর দিয়ে পরীক্ষা করুন
                        </p>
                    </div>

                    <div class="action-buttons">
                        <a href="@Url.Action("Login", "Home")" class="btn btn-outline">
                            <i class="fas fa-arrow-left"></i>
                            লগইনে ফিরে যান
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-arrow-right"></i>
                            পরবর্তী ধাপ
                        </button>
                    </div>
                </form>

                <div class="login-link">
                    <p>
                        ইতিমধ্যে একাউন্ট আছে?
                        <a href="@Url.Action("Login", "Home")">লগইন করুন</a>
                    </p>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="registration-footer">
        <div style="max-width: 1400px; margin: 0 auto; padding: 0 1.5rem;">
            <p>&copy; <script>document.write(new Date().getFullYear());</script> ঠিকানা যাচাইকরণ সিস্টেম - বাংলাদেশ পুলিশ। সকল অধিকার সংরক্ষিত।</p>
        </div>
    </footer>

    <script>
        // Fill demo credentials
        function fillDemoCredentials(nid, mobile) {
            document.querySelector('input[name="idNumber"]').value = nid;
            document.querySelector('input[name="mobileNumber"]').value = mobile;
        }

        // Input auto-formatting
        document.addEventListener('DOMContentLoaded', function() {
            // Format mobile number
            document.querySelector('input[name="mobileNumber"]').addEventListener('input', function(e) {
                let value = e.target.value.replace(/\D/g, '');
                if (value.length > 11) value = value.slice(0, 11);
                e.target.value = value;
            });

            // Format NID
            document.querySelector('input[name="idNumber"]').addEventListener('input', function(e) {
                let value = e.target.value.replace(/\D/g, '');
                if (value.length > 17) value = value.slice(0, 17);
                e.target.value = value;
            });

            // Auto-fill demo for testing
            fillDemoCredentials('1990123456789', '01712345678');
        });
    </script>
</body>
</html>