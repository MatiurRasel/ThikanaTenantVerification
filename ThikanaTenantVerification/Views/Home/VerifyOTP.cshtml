<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OTP যাচাই - ঠিকানা যাচাইকরণ</title>
    <link href="https://fonts.maateen.me/solaiman-lipi/font.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --police-blue: #0d2b6c;
            --police-red: #e4002b;
            --police-gold: #ffc107;
            --police-light-blue: #1e88e5;
            --dark-bg: #0a1a36;
            --light-bg: #f0f4f8;
            --white: #ffffff;
            --text-dark: #1e293b;
            --text-light: #64748b;
            --success-color: #10b981;
            --error-color: #ef4444;
            --warning-color: #f59e0b;
            --gradient-blue: linear-gradient(135deg, #0d2b6c 0%, #1e3a8a 100%);
            --gradient-gold: linear-gradient(135deg, #ffc107 0%, #ffd54f 100%);
            --shadow-strong: 0 20px 60px rgba(0, 0, 0, 0.3);
            --shadow-light: 0 8px 30px rgba(0, 0, 0, 0.1);
            --radius-lg: 20px;
            --radius-md: 12px;
            --radius-sm: 8px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'SolaimanLipi', 'Inter', sans-serif;
            background: var(--gradient-blue);
            color: var(--text-dark);
            line-height: 1.6;
            min-height: 100vh;
        }

        /* Compact Header */
        .police-header {
            background: rgba(13, 43, 108, 0.98);
            backdrop-filter: blur(10px);
            border-bottom: 2px solid var(--police-red);
            position: sticky;
            top: 0;
            z-index: 1000;
            padding: 0.6rem 0;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        .header-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            text-decoration: none;
        }

        .police-emblem {
            width: 45px;
            height: 45px;
            background: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid var(--police-gold);
        }

        .police-emblem i {
            color: var(--police-blue);
            font-size: 1.4rem;
        }

        .logo-text {
            color: var(--white);
            line-height: 1.2;
        }

        .logo-main {
            font-size: 1.3rem;
            font-weight: 700;
            display: block;
        }

        .logo-sub {
            font-size: 0.8rem;
            opacity: 0.9;
        }

        /* Main Content */
        .main-content {
            min-height: calc(100vh - 120px);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
        }

        /* OTP Container */
        .otp-container {
            width: 100%;
            max-width: 500px;
        }

        .otp-card {
            background: var(--white);
            border-radius: var(--radius-lg);
            padding: 2.5rem;
            box-shadow: var(--shadow-strong);
        }

        /* Progress Steps */
        .progress-steps {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 2rem;
        }

        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            z-index: 1;
        }

        .step-number {
            width: 40px;
            height: 40px;
            background: #e2e8f0;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            color: var(--text-light);
            margin-bottom: 0.5rem;
            transition: all 0.3s ease;
        }

        .step.active .step-number {
            background: var(--police-blue);
            color: var(--white);
            transform: scale(1.1);
        }

        .step.completed .step-number {
            background: var(--success-color);
            color: var(--white);
        }

        .step-label {
            font-size: 0.8rem;
            color: var(--text-light);
            text-align: center;
        }

        .step-line {
            flex: 1;
            height: 2px;
            background: #e2e8f0;
            margin: 0 1rem;
            position: relative;
            top: -20px;
        }

        /* Header */
        .otp-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .otp-icon {
            width: 80px;
            height: 80px;
            background: var(--gradient-gold);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            font-size: 2.5rem;
            color: var(--police-blue);
            animation: pulse 2s infinite;
        }

        @@keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        .otp-title {
            font-size: 1.8rem;
            color: var(--police-blue);
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .otp-subtitle {
            color: var(--text-light);
            margin-bottom: 1rem;
        }

        /* Mobile Info */
        .mobile-info {
            background: rgba(13, 43, 108, 0.05);
            border-radius: var(--radius-md);
            padding: 1rem;
            margin-bottom: 1.5rem;
            text-align: center;
            border: 1px solid rgba(13, 43, 108, 0.1);
        }

        .mobile-number {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--police-blue);
            letter-spacing: 1px;
        }

        /* Demo OTP */
        .demo-otp {
            background: linear-gradient(135deg, #0d2b6c 0%, #1e88e5 100%);
            border-radius: var(--radius-md);
            padding: 1.5rem;
            margin-bottom: 2rem;
            color: var(--white);
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .demo-header {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            margin-bottom: 1rem;
            font-size: 1.1rem;
            font-weight: 600;
        }

        .demo-code {
            background: rgba(255, 255, 255, 0.2);
            padding: 1rem;
            border-radius: var(--radius-sm);
            margin-bottom: 0.75rem;
        }

        .demo-code code {
            font-family: 'Courier New', monospace;
            font-size: 2.5rem;
            font-weight: 800;
            letter-spacing: 0.5rem;
            color: var(--police-gold);
        }

        .demo-info {
            font-size: 0.9rem;
            opacity: 0.9;
        }

        /* OTP Input */
        .otp-input-container {
            margin: 2rem 0;
        }

        .otp-inputs {
            display: flex;
            justify-content: center;
            gap: 0.75rem;
            margin-bottom: 1rem;
        }

        .otp-digit {
            width: 60px;
            height: 70px;
            text-align: center;
            font-size: 2rem;
            font-weight: 700;
            border: 2px solid #e2e8f0;
            border-radius: var(--radius-sm);
            transition: all 0.3s ease;
        }

        .otp-digit:focus {
            border-color: var(--police-blue);
            box-shadow: 0 0 0 3px rgba(13, 43, 108, 0.1);
            outline: none;
        }

        .otp-hint {
            text-align: center;
            color: var(--text-light);
            font-size: 0.9rem;
        }

        /* Timer */
        .otp-timer {
            text-align: center;
            margin: 1.5rem 0;
            color: var(--text-light);
            font-size: 0.95rem;
        }

        .timer-value {
            font-weight: 700;
            color: var(--police-red);
            font-size: 1.2rem;
        }

        /* Password Section */
        .password-section {
            display: none;
            margin-top: 2rem;
            animation: fadeIn 0.5s ease;
        }

        .password-section.show {
            display: block;
        }

        .section-header {
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .section-title {
            font-size: 1.3rem;
            color: var(--police-blue);
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .form-group {
            margin-bottom: 1.25rem;
        }

        .form-label {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-weight: 600;
            color: var(--police-blue);
            margin-bottom: 0.5rem;
            font-size: 0.95rem;
        }

        .input-group {
            position: relative;
        }

        .input-group i {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-light);
            font-size: 1.1rem;
        }

        .form-control {
            width: 100%;
            padding: 0.875rem 1rem 0.875rem 3rem;
            border: 2px solid #e2e8f0;
            border-radius: var(--radius-sm);
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--police-blue);
            box-shadow: 0 0 0 3px rgba(13, 43, 108, 0.1);
        }

        /* Password Strength */
        .password-strength {
            margin-top: 0.75rem;
        }

        .strength-meter {
            height: 6px;
            background: #e2e8f0;
            border-radius: 3px;
            overflow: hidden;
            margin-bottom: 0.5rem;
        }

        .strength-fill {
            height: 100%;
            width: 0%;
            background: var(--error-color);
            border-radius: 3px;
            transition: all 0.3s ease;
        }

        .strength-text {
            font-size: 0.8rem;
            color: var(--text-light);
        }

        /* Password Requirements */
        .password-requirements {
            background: #f8fafc;
            border-radius: var(--radius-sm);
            padding: 1.25rem;
            margin-top: 1rem;
            border: 1px solid #e2e8f0;
        }

        .requirements-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.75rem;
        }

        .requirement-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.85rem;
            color: var(--text-light);
        }

        .requirement-item.valid {
            color: var(--success-color);
        }

        /* Buttons */
        .action-buttons {
            display: flex;
            gap: 1rem;
            margin-top: 2rem;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.75rem;
            padding: 0.875rem 1.75rem;
            border-radius: var(--radius-md);
            font-weight: 600;
            font-size: 1rem;
            text-decoration: none;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            flex: 1;
        }

        .btn-primary {
            background: var(--gradient-blue);
            color: var(--white);
            box-shadow: 0 10px 25px rgba(13, 43, 108, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 35px rgba(13, 43, 108, 0.4);
        }

        .btn-secondary {
            background: transparent;
            color: var(--police-blue);
            border: 2px solid var(--police-blue);
        }

        .btn-secondary:hover {
            background: var(--police-blue);
            color: var(--white);
        }

        .btn-success {
            background: var(--success-color);
            color: var(--white);
            box-shadow: 0 10px 25px rgba(16, 185, 129, 0.3);
        }

        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 35px rgba(16, 185, 129, 0.4);
        }

        /* Alert */
        .alert {
            padding: 1rem;
            border-radius: var(--radius-sm);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            animation: slideIn 0.5s ease;
        }

        .alert-danger {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            color: var(--error-color);
        }

        .alert-success {
            background: rgba(16, 185, 129, 0.1);
            border: 1px solid rgba(16, 185, 129, 0.2);
            color: var(--success-color);
        }

        /* Quick Actions */
        .quick-actions {
            display: flex;
            gap: 0.75rem;
            margin-top: 1.5rem;
        }

        .quick-btn {
            flex: 1;
            background: #f1f5f9;
            border: 1px solid #e2e8f0;
            border-radius: var(--radius-sm);
            padding: 0.75rem;
            font-size: 0.85rem;
            color: var(--police-blue);
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .quick-btn:hover {
            background: #e2e8f0;
            transform: translateY(-1px);
        }

        /* Footer */
        .otp-footer {
            background: var(--dark-bg);
            color: var(--white);
            padding: 1rem 0;
            text-align: center;
            font-size: 0.85rem;
            margin-top: 2rem;
        }

        /* Animations */
        @@keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @@keyframes slideIn {
            from { opacity: 0; transform: translateX(-20px); }
            to { opacity: 1; transform: translateX(0); }
        }

        /* Responsive */
        @@media (max-width: 768px) {
            .otp-card {
                padding: 1.5rem;
            }
            
            .otp-digit {
                width: 50px;
                height: 60px;
                font-size: 1.8rem;
            }
            
            .demo-code code {
                font-size: 2rem;
                letter-spacing: 0.3rem;
            }
            
            .requirements-grid {
                grid-template-columns: 1fr;
            }
            
            .action-buttons {
                flex-direction: column;
            }
            
            .btn {
                width: 100%;
            }
            
            .header-container {
                padding: 0 1rem;
            }
        }

        @@media (max-height: 768px) {
            .main-content {
                padding: 1rem;
                min-height: calc(100vh - 100px);
            }
            
            .otp-card {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Compact Header -->
    <header class="police-header">
        <div class="header-container">
            <a href="/" class="logo-container">
                <div class="police-emblem">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <div class="logo-text">
                    <span class="logo-main">ঠিকানা যাচাইকরণ</span>
                    <span class="logo-sub">বাংলাদেশ পুলিশ</span>
                </div>
            </a>
        </div>
    </header>

    <!-- Main Content -->
    <main class="main-content">
        <div class="otp-container">
            <div class="otp-card">
                <!-- Progress Steps -->
                <div class="progress-steps">
                    <div class="step @(isNewUser ? "completed" : "active")">
                        <div class="step-number">১</div>
                        <div class="step-label">@(isNewUser ? "NID যাচাই" : "লগইন")</div>
                    </div>
                    <div class="step-line"></div>
                    <div class="step active">
                        <div class="step-number">২</div>
                        <div class="step-label">OTP যাচাই</div>
                    </div>
                    <div class="step-line"></div>
                    <div class="step @(isNewUser ? "" : "disabled")">
                        <div class="step-number">৩</div>
                        <div class="step-label">@(isNewUser ? "পাসওয়ার্ড" : "ড্যাশবোর্ড")</div>
                    </div>
                </div>

                <!-- Header -->
                <div class="otp-header">
                    <div class="otp-icon">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <h2 class="otp-title">OTP যাচাই করুন</h2>
                    <p class="otp-subtitle">
                        @if (isNewUser)
                        {
                            <span>নতুন ব্যবহারকারী নিবন্ধন</span>
                        }
                        else
                        {
                            <span>অ্যাকাউন্ট লগইন</span>
                        }
                    </p>
                </div>

                <!-- Mobile Info -->
                <div class="mobile-info">
                    <p>OTP পাঠানো হয়েছে নিচের নম্বরে:</p>
                    <div class="mobile-number">@mobileNumber</div>
                </div>

                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger">
                        <i class="fas fa-exclamation-circle"></i>
                        @ViewBag.Error
                    </div>
                }

                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-success">
                        <i class="fas fa-check-circle"></i>
                        @TempData["Success"]
                    </div>
                }

                <!-- Demo OTP -->
                <div class="demo-otp">
                    <div class="demo-header">
                        <i class="fas fa-code"></i>
                        <span>ডেমো OTP কোড</span>
                    </div>
                    <div class="demo-code">
                        <code>123456</code>
                    </div>
                    <p class="demo-info">ডেভেলপমেন্টের জন্য এই কোডটি ব্যবহার করুন</p>
                </div>

                <!-- OTP Input Form -->
                <form method="post" action="@Url.Action("VerifyOTP", "Home")" id="otpForm">
                    <input type="hidden" name="isNewUser" value="@isNewUser" />
                    @if (userId != null)
                    {
                        <input type="hidden" name="userId" value="@userId" />
                    }

                    <div class="otp-input-container">
                        <div class="otp-inputs">
                            <input type="text" class="otp-digit" maxlength="1" pattern="\d" oninput="moveToNext(this, 1)">
                            <input type="text" class="otp-digit" maxlength="1" pattern="\d" oninput="moveToNext(this, 2)">
                            <input type="text" class="otp-digit" maxlength="1" pattern="\d" oninput="moveToNext(this, 3)">
                            <input type="text" class="otp-digit" maxlength="1" pattern="\d" oninput="moveToNext(this, 4)">
                            <input type="text" class="otp-digit" maxlength="1" pattern="\d" oninput="moveToNext(this, 5)">
                            <input type="text" class="otp-digit" maxlength="1" pattern="\d" oninput="moveToNext(this, 6)">
                        </div>
                        <input type="hidden" name="otp" id="otpValue">
                        <p class="otp-hint">ডেমো OTP: <strong>123456</strong></p>
                    </div>

                    <!-- Timer -->
                    <div class="otp-timer">
                        OTP কোডটি <span class="timer-value" id="timer">05:00</span> মিনিটের জন্য বৈধ
                    </div>

                    <!-- Password Section (For New Users Only) -->
                    @if (isNewUser)
                    {
                        <div id="passwordSection" class="password-section">
                            <div class="section-header">
                                <h3 class="section-title">নিরাপদ পাসওয়ার্ড সেট করুন</h3>
                                <p class="otp-subtitle">নিচের নির্দেশনা অনুসরণ করুন</p>
                            </div>

                            <div class="form-group">
                                <label class="form-label">
                                    <i class="fas fa-lock"></i>
                                    পাসওয়ার্ড
                                </label>
                                <div class="input-group">
                                    <i class="fas fa-key"></i>
                                    <input type="password"
                                           name="password"
                                           id="password"
                                           class="form-control"
                                           placeholder="পাসওয়ার্ড দিন"
                                           minlength="8"
                                           required
                                           oninput="validatePassword()">
                                </div>
                                <div class="password-strength">
                                    <div class="strength-meter">
                                        <div class="strength-fill" id="strengthFill"></div>
                                    </div>
                                    <div class="strength-text" id="strengthText">পাসওয়ার্ড শক্তি</div>
                                </div>
                            </div>

                            <div class="form-group">
                                <label class="form-label">
                                    <i class="fas fa-lock"></i>
                                    পাসওয়ার্ড নিশ্চিত করুন
                                </label>
                                <div class="input-group">
                                    <i class="fas fa-key"></i>
                                    <input type="password"
                                           name="confirmPassword"
                                           id="confirmPassword"
                                           class="form-control"
                                           placeholder="পুনরায় পাসওয়ার্ড দিন"
                                           minlength="8"
                                           required
                                           oninput="checkPasswordMatch()">
                                </div>
                                <div id="passwordMatch" style="margin-top: 0.5rem; font-size: 0.85rem;">
                                    <span id="matchText"></span>
                                </div>
                            </div>

                            <!-- Password Requirements -->
                            <div class="password-requirements">
                                <div style="margin-bottom: 0.75rem; color: var(--police-blue); font-weight: 600;">
                                    <i class="fas fa-list-check"></i> পাসওয়ার্ডের প্রয়োজনীয়তা
                                </div>
                                <div class="requirements-grid">
                                    <div class="requirement-item" id="req-length">
                                        <i class="fas fa-circle"></i>
                                        <span>কমপক্ষে ৮ অক্ষর</span>
                                    </div>
                                    <div class="requirement-item" id="req-uppercase">
                                        <i class="fas fa-circle"></i>
                                        <span>অন্তত ১টি বড় হাতের অক্ষর</span>
                                    </div>
                                    <div class="requirement-item" id="req-lowercase">
                                        <i class="fas fa-circle"></i>
                                        <span>অন্তত ১টি ছোট হাতের অক্ষর</span>
                                    </div>
                                    <div class="requirement-item" id="req-number">
                                        <i class="fas fa-circle"></i>
                                        <span>অন্তত ১টি সংখ্যা</span>
                                    </div>
                                    <div class="requirement-item" id="req-special">
                                        <i class="fas fa-circle"></i>
                                        <span>অন্তত ১টি বিশেষ অক্ষর</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }

                    <!-- Action Buttons -->
                    <div class="action-buttons">
                        <a href="@Url.Action("Login", "Home")" class="btn btn-secondary">
                            <i class="fas fa-arrow-left"></i>
                            ফিরে যান
                        </a>
                        
                        @if (isNewUser)
                        {
                            <button type="button" id="nextBtn" class="btn btn-primary" onclick="showPasswordSection()">
                                <i class="fas fa-arrow-right"></i>
                                পরবর্তী
                            </button>
                            <button type="submit" id="submitBtn" class="btn btn-success" style="display: none;">
                                <i class="fas fa-user-plus"></i>
                                রেজিস্ট্রেশন সম্পূর্ণ করুন
                            </button>
                        }
                        else
                        {
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-check-circle"></i>
                                যাচাই করুন
                            </button>
                        }
                    </div>
                </form>

                <!-- Quick Actions -->
                <div class="quick-actions">
                    <button type="button" class="quick-btn" onclick="fillDemoOTP()">
                        <i class="fas fa-magic"></i>
                        ডেমো OTP বসান
                    </button>
                    <button type="button" class="quick-btn" onclick="autoSubmit()">
                        <i class="fas fa-bolt"></i>
                        অটো যাচাই
                    </button>
                    @if (isNewUser)
                    {
                        <button type="button" class="quick-btn" onclick="generatePassword()">
                            <i class="fas fa-dice"></i>
                            পাসওয়ার্ড জেনারেট করুন
                        </button>
                    }
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="otp-footer">
        <div style="max-width: 1400px; margin: 0 auto; padding: 0 1.5rem;">
            <p>&copy; <script>document.write(new Date().getFullYear());</script> ঠিকানা যাচাইকরণ সিস্টেম - বাংলাদেশ পুলিশ। সকল অধিকার সংরক্ষিত।</p>
        </div>
    </footer>

    <script>
        let timeLeft = 300; // 5 minutes
        let timerInterval = null;

        // Initialize OTP inputs
        document.addEventListener('DOMContentLoaded', function() {
            // Focus first OTP input
            document.querySelector('.otp-digit').focus();
            
            // Start timer
            startTimer();
            
            // Update OTP value on input
            updateOTPValue();
        });

        // Move to next input
        function moveToNext(input, index) {
            if (input.value.length === 1) {
                const inputs = document.querySelectorAll('.otp-digit');
                if (index < inputs.length) {
                    inputs[index].focus();
                }
            }
            updateOTPValue();
            
            // Auto-submit if 6 digits entered and not new user
            const otp = document.getElementById('otpValue').value;
            const isNewUser = document.querySelector('input[name="isNewUser"]').value === 'True';
            
            if (otp.length === 6 && !isNewUser) {
                setTimeout(() => {
                    document.getElementById('otpForm').submit();
                }, 500);
            }
        }

        // Update OTP value
        function updateOTPValue() {
            const inputs = document.querySelectorAll('.otp-digit');
            let otp = '';
            inputs.forEach(input => {
                otp += input.value;
            });
            document.getElementById('otpValue').value = otp;
        }

        // Fill demo OTP
        function fillDemoOTP() {
            const inputs = document.querySelectorAll('.otp-digit');
            const demoOTP = '123456';
            
            inputs.forEach((input, index) => {
                if (index < demoOTP.length) {
                    input.value = demoOTP[index];
                    input.dispatchEvent(new Event('input'));
                }
            });
            
            // Show success animation
            inputs.forEach(input => {
                input.style.borderColor = '#10b981';
                input.style.boxShadow = '0 0 0 3px rgba(16, 185, 129, 0.1)';
                
                setTimeout(() => {
                    input.style.borderColor = '';
                    input.style.boxShadow = '';
                }, 1000);
            });
        }

        // Start timer
        function startTimer() {
            const timerElement = document.getElementById('timer');
            
            timerInterval = setInterval(() => {
                const minutes = Math.floor(timeLeft / 60);
                const seconds = timeLeft % 60;
                
                timerElement.textContent = `${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
                
                if (timeLeft <= 0) {
                    clearInterval(timerInterval);
                    timerElement.textContent = '00:00';
                    timerElement.style.color = '#ef4444';
                } else {
                    timeLeft--;
                }
            }, 1000);
        }

        // Show password section for new users
        function showPasswordSection() {
            const otp = document.getElementById('otpValue').value;
            const isNewUser = document.querySelector('input[name="isNewUser"]').value === 'True';
            
            if (!isNewUser) return;
            
            if (otp.length !== 6) {
                showMessage('অনুগ্রহ করে ৬ ডিজিটের OTP দিন', 'error');
                return;
            }
            
            const passwordSection = document.getElementById('passwordSection');
            const nextBtn = document.getElementById('nextBtn');
            const submitBtn = document.getElementById('submitBtn');
            
            // Show password section
            passwordSection.classList.add('show');
            passwordSection.style.display = 'block';
            
            // Switch buttons
            nextBtn.style.display = 'none';
            submitBtn.style.display = 'flex';
            
            // Focus on password field
            setTimeout(() => {
                document.getElementById('password').focus();
            }, 100);
        }

        // Auto submit
        function autoSubmit() {
            fillDemoOTP();
            
            const isNewUser = document.querySelector('input[name="isNewUser"]').value === 'True';
            
            if (isNewUser) {
                setTimeout(() => {
                    showPasswordSection();
                    generatePassword();
                }, 500);
            } else {
                setTimeout(() => {
                    document.getElementById('otpForm').submit();
                }, 500);
            }
        }

        // Generate password
        function generatePassword() {
            const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@@#$%^&*';
            let password = '';
            
            // Ensure requirements
            password += 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'[Math.floor(Math.random() * 26)];
            password += 'abcdefghijklmnopqrstuvwxyz'[Math.floor(Math.random() * 26)];
            password += '0123456789'[Math.floor(Math.random() * 10)];
            password += '!@@#$%^&*'[Math.floor(Math.random() * 8)];
            
            // Add remaining
            for (let i = 0; i < 4; i++) {
                password += chars[Math.floor(Math.random() * chars.length)];
            }
            
            // Shuffle
            password = password.split('').sort(() => Math.random() - 0.5).join('');
            
            // Set values
            document.getElementById('password').value = password;
            document.getElementById('confirmPassword').value = password;
            
            // Trigger validation
            validatePassword();
            checkPasswordMatch();
            
            showMessage('পাসওয়ার্ড জেনারেট করা হয়েছে!', 'success');
        }

        // Validate password
        function validatePassword() {
            const password = document.getElementById('password').value;
            
            const requirements = {
                length: password.length >= 8,
                uppercase: /[A-Z]/.test(password),
                lowercase: /[a-z]/.test(password),
                number: /[0-9]/.test(password),
                special: /[!@@#$%^&*]/.test(password)
            };
            
            // Update indicators
            Object.keys(requirements).forEach(key => {
                const element = document.getElementById(`req-${key}`);
                if (requirements[key]) {
                    element.classList.add('valid');
                    element.innerHTML = '<i class="fas fa-check-circle"></i> ' + element.textContent;
                } else {
                    element.classList.remove('valid');
                    element.innerHTML = '<i class="fas fa-circle"></i> ' + element.textContent;
                }
            });
            
            // Update strength meter
            let strength = 0;
            Object.values(requirements).forEach(isMet => {
                if (isMet) strength += 20;
            });
            
            const strengthFill = document.getElementById('strengthFill');
            const strengthText = document.getElementById('strengthText');
            
            strengthFill.style.width = strength + '%';
            
            if (strength < 40) {
                strengthFill.style.background = '#ef4444';
                strengthText.textContent = 'দুর্বল পাসওয়ার্ড';
                strengthText.style.color = '#ef4444';
            } else if (strength < 80) {
                strengthFill.style.background = '#f59e0b';
                strengthText.textContent = 'মাঝারি পাসওয়ার্ড';
                strengthText.style.color = '#f59e0b';
            } else {
                strengthFill.style.background = '#10b981';
                strengthText.textContent = 'শক্তিশালী পাসওয়ার্ড';
                strengthText.style.color = '#10b981';
            }
        }

        // Check password match
        function checkPasswordMatch() {
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirmPassword').value;
            const matchDiv = document.getElementById('matchText');
            
            if (password && confirmPassword) {
                if (password === confirmPassword) {
                    matchDiv.innerHTML = '<i class="fas fa-check-circle" style="color: #10b981;"></i> পাসওয়ার্ড মিলেছে';
                } else {
                    matchDiv.innerHTML = '<i class="fas fa-times-circle" style="color: #ef4444;"></i> পাসওয়ার্ড মিলছে না';
                }
            } else {
                matchDiv.innerHTML = '';
            }
        }

        // Show message
        function showMessage(message, type) {
            const alertDiv = document.createElement('div');
            alertDiv.className = `alert alert-${type === 'error' ? 'danger' : 'success'}`;
            alertDiv.innerHTML = `
                <i class="fas fa-${type === 'error' ? 'exclamation-circle' : 'check-circle'}"></i>
                ${message}
            `;
            
            const form = document.getElementById('otpForm');
            form.insertBefore(alertDiv, form.firstChild);
            
            setTimeout(() => {
                alertDiv.remove();
            }, 3000);
        }

        // Form validation
        document.getElementById('otpForm').addEventListener('submit', function(e) {
            const isNewUser = document.querySelector('input[name="isNewUser"]').value === 'True';
            const otp = document.getElementById('otpValue').value;
            
            // Always validate OTP
            if (otp.length !== 6) {
                e.preventDefault();
                showMessage('অনুগ্রহ করে ৬ ডিজিটের OTP দিন', 'error');
                return;
            }
            
            if (isNewUser) {
                const passwordSection = document.getElementById('passwordSection');
                
                // If password section is hidden, show it and prevent submit
                if (!passwordSection.classList.contains('show')) {
                    e.preventDefault();
                    showPasswordSection();
                    return;
                }
                
                // Validate passwords
                const password = document.getElementById('password').value;
                const confirmPassword = document.getElementById('confirmPassword').value;
                
                if (password.length < 8) {
                    e.preventDefault();
                    showMessage('পাসওয়ার্ড কমপক্ষে ৮ অক্ষরের হতে হবে', 'error');
                    return;
                }
                
                if (password !== confirmPassword) {
                    e.preventDefault();
                    showMessage('পাসওয়ার্ড মিলছে না', 'error');
                    return;
                }
                
                // Check requirements
                const hasUppercase = /[A-Z]/.test(password);
                const hasLowercase = /[a-z]/.test(password);
                const hasNumber = /[0-9]/.test(password);
                const hasSpecial = /[!@@#$%^&*]/.test(password);
                
                if (!hasUppercase || !hasLowercase || !hasNumber || !hasSpecial) {
                    e.preventDefault();
                    showMessage('পাসওয়ার্ড প্রয়োজনীয়তা পূরণ হয়নি', 'error');
                    return;
                }
            }
        });
    </script>
</body>
</html>